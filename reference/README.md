# Reference, temporary

The `main.ts` of the two beacons as they wire the core by hand today, kept here only so the facade (docs/beacon-library.md section 4) can be built against the real call sites. The task that builds the facade deletes this folder.
